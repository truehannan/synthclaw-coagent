{
  "name": "synthclaw-coagent",
  "version": "1.0.0",
  "private": true,
  "description": "SynthClaw CoAgent — Personal AI Agent",
  "scripts": {
    "setup": "cd cli && npm install && npm run build && npm link && echo '✓ synthclaw command ready'",
    "install": "cd cli && npm install",
    "build": "cd cli && npm install && npm run build",
    "link": "cd cli && npm install && npm run build && npm link",
    "postinstall": "cd cli && npm run build 2>/dev/null || true"
  },
  "engines": {
    "node": ">=18"
  }
}
