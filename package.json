{
  "name": "synthclaw-coagent",
  "version": "1.0.0",
  "private": true,
  "description": "SynthClaw CoAgent — Personal AI Agent",
  "scripts": {
    "setup": "cd cli && npm install && npm run build && npm link",
    "build": "cd cli && npm install && npm run build",
    "link": "cd cli && npm link"
  },
  "engines": {
    "node": ">=18"
  }
}
